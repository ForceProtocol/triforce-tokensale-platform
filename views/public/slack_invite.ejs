<%- partial('./partials/header.ejs') %>

<header class="padding-top80 white-bg">

	<div class="container white-bg">

		<div class="banner-content text-center animation animated animation-fade-in-down" data-animation="animation-fade-in-down">
			<a href="/"><img src="/images/triforce-tokens-banner-logo-2.png" alt="TriForce Tokens Ltd Logo" /></a>
		</div>
		
	</div>

</header>

<div class="section">

	<div class="container">
	
		<div class="col-sm-12 col-md-12 col-lg-12">
			
			<div class="text-center">
				<img class="img-responsive center-block" src="/images/brand-logos/slack-logo.png" alt="Slack Logo" />
			</div>
		

            <% if(flash.has('errors')){ %>
			<br /><br />
            <div class="alert alert-danger col-lg-12 col-md-12 col-sm-12">
                <% flash.get('errors').forEach(function (message) { %>
                <h4 class="error"><i class="fa fa-exclamation-triangle"></i> &nbsp;&nbsp;<%- message %></h4>
                <% }) %>
            </div>
            <% } %>

            <% if(flash.has('success')){ %>
			<br /><br />
            <div class="alert alert-success col-lg-12 col-md-12 col-sm-12">
                <% flash.get('success').forEach(function (message) { %>
                <h4 class="success"><i class="fa fa-exclamation-check"></i> &nbsp;&nbsp;<%- message %></h4>
                <% }) %>
            </div>
            <% } %>

			
			<div class="form-container subscribe-form-container col-lg-8 col-md-8 col-sm-12 col-lg-offset-2 col-md-offset-2">
				<h2 class="subscribe-form-heading">please enter your email below to join triforce tokens on slack</h2>
				<form id="slackInvite" class="form-inline" method="POST" action="/slack-invite">
					<div class="form-group form-group-lg">
						<input required="required" placeholder="ENTER YOUR EMAIL" class="form-control" type="email" name="email" value="" />
					</div>
					<div class="form-group form-group-lg">
						<button type="submit" class="btn btn-block btn-cta presale-subscribe-btn">JOIN SLACK</button>
					</div>
				</form>
			</div>
		
		</div>
		
		
	</div>

</div>

<br />
<br />
<br />
<%- partial('./partials/footer.ejs') %>